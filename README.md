# WordHunter 🎯

一款结合现实互动的英语单词"寻宝" Web App。通过拍摄现实中的物体来学习英语单词，让英语学习变得有趣又高效！

## 🌐 在线体验

**📱 手机端访问体验地址：https://wordshunter.online**

> 推荐使用手机浏览器访问，获得最佳拍照体验

## ✨ 功能特点

- **分屏交互设计**: 上方显示单词和收集网格，下方是相机区域
- **AI 物体识别**: 支持多种视觉大模型（豆包/Gemini/OpenRouter）
- **游戏化学习**: 拍摄对应物体完成单词学习任务
- **难度分级**: 200+ 词库，分为 3 个难度等级
- **精美动画**: 使用 Framer Motion 实现流畅的交互动画
- **单词本**: 记录已学习的单词，支持复习模式
- **用户系统**: 支持登录注册，数据云端同步
- **成就系统**: 完成学习任务解锁成就徽章

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.local.example` 为 `.env.local` 并填入你的 API 密钥：

```bash
cp .env.local.example .env.local
```

配置项：
- `DOUBAO_API_KEY`: 豆包视觉模型 API Key（大陆服务器推荐）
- `GEMINI_API_KEY`: Gemini API Key（海外服务器）
- `OPENROUTER_API_KEY`: OpenRouter API Key（全球可访问，推荐）

> 💡 如果不配置 API Key，将使用模拟模式进行开发测试

### 3. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 开始游戏！

## 📱 使用方法

1. 查看当前单词（如 "CUP"）
2. 点击"开始寻找"按钮激活相机
3. 在现实中找到对应物体并拍照
4. AI 会判断拍摄的物体是否匹配
5. 完成匹配后获得碎片奖励
6. 继续下一个单词的学习！

## 🛠 技术栈

- **Frontend**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **Animation**: Framer Motion
- **Icons**: Lucide React
- **AI Vision**: 豆包 / Gemini / OpenRouter (可切换)
- **Database**: Supabase
- **Auth**: Supabase Auth

## 📁 项目结构

```
src/
├── app/
│   ├── api/
│   │   ├── recognize/    # AI 物体识别 API
│   │   ├── removebg/     # 背景移除 API
│   │   └── segment/      # 图像分割 API
│   ├── globals.css       # 全局样式
│   ├── layout.tsx        # 根布局
│   └── page.tsx          # 主页面
├── components/
│   ├── ActionArea.tsx    # 底部交互区
│   ├── AchievementToast.tsx # 成就提示
│   ├── AuthModal.tsx     # 登录/注册弹窗
│   ├── BgmHost.tsx       # 背景音乐控制
│   ├── BottomNav.tsx     # 底部导航栏
│   ├── CameraView.tsx    # 相机视图
│   ├── CollectionGrid.tsx # 收集网格
│   ├── ProfilePage.tsx   # 个人主页
│   ├── ReviewMode.tsx    # 复习模式
│   ├── SplashScreen.tsx  # 启动画面
│   ├── VictoryModal.tsx  # 胜利弹窗
│   ├── WordBook.tsx      # 单词本
│   └── WordCard.tsx      # 单词卡片
├── context/              # React Context
├── data/
│   └── wordBank.ts       # 词库数据 (200+ 单词)
├── hooks/                # 自定义 Hooks
├── lib/                  # 工具库
└── types/                # TypeScript 类型定义
```

## 🎮 游戏机制

### 难度等级
- ⭐ 简单: 常见日常物品 (80+ 词)
- ⭐⭐ 中等: 一般难度词汇 (70+ 词)
- ⭐⭐⭐ 困难: 较难/不常见词汇 (50+ 词)

### 词汇分类
食物、日用品、学习用品、家具、衣物、电子产品、动物、自然、玩具、厨房用品、工具等

### AI 识别配置
在 `src/app/api/recognize/route.ts` 中可切换 AI 提供商：
```typescript
// 0 = 豆包API, 1 = Gemini API, 2 = OpenRouter API
const API_PROVIDER = 2;  // 推荐使用 OpenRouter
```

## 🔧 部署

### Vercel 部署（推荐）

1. Fork 本项目
2. 在 Vercel 中导入项目
3. 配置环境变量（Settings → Environment Variables）
4. 部署完成

## 📄 License

MIT
